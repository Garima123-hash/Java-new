let obj={
    item:"pen",
    price:10
}
console.log(`the cost of ${obj.item} is ${obj.price}`)//template literal called because special type of sstirng 
//things inside${} first calculate value and then tske it as string
//ex:${1+2}-vlue calcul phir
//string methods:
//let str="Apna"
// str.length
//These metrhods do not change the original string so thats why they need to be stored in another string
//str.toUpperCase()
//str.toLowerCase()
//str.trim()-remove white soace from start and end.
//strings in java script are immutable
//str.concat(another tsring )
//str.slice(start,end)
//str.replace(search,new)
//str.charAt(index)-value at that index